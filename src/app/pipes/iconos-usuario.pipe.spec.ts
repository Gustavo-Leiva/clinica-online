import { IconosUsuarioPipe } from './iconos-usuario.pipe';

describe('IconosUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new IconosUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
