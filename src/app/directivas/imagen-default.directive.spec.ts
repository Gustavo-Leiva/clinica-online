import { ImagenDefaultDirective } from './imagen-default.directive';

describe('ImagenDefaultDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagenDefaultDirective();
    expect(directive).toBeTruthy();
  });
});
