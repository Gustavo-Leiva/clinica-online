import { ExpandirHoverDirective } from './expandir-hover.directive';

describe('ExpandirHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandirHoverDirective();
    expect(directive).toBeTruthy();
  });
});
